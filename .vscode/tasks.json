{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "gcc -Icjson -o ${fileDirname}/${fileBasenameNoExtension} ${file} cjson/cJSON.c",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}